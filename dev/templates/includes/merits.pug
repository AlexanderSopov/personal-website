section(id=page.title, class='page')
	.title
		h1=page.title+"."
	.content-wrap
		- for(var j=0; j<page.content.length;j++)
			- var content = page.content[j];
			div(id="scroll-"+j, class=j==0 ? 'module active' : 'module')
				h2=content.h1
				h3=content.h2
				p=content.p[0]
