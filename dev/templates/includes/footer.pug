//- section.page
//-   .container
//-     .logo
//-       h3=logo
//-     .email
//-       h3=contact.email
//-     .github
//-       h3=contact.github
//-     .copyright
//-       h3=copyright


section(class=i==0 ? 'page active' : 'page')#footer
  .title
    h1=page.title+"."
  .content-wrap
    - for(var j=0; j<page.content.length;j++)
      - var content = page.content[j];
      div(id=content.h1, class=j==0 ? 'module active' : 'module')
        h2=content.h1
        h3=content.h2
        - for(var k=0; k<content.p.length;k++)
          - var p = content.p[k];
            p=p